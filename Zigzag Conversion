class Solution:
    def convert(self, s: str, numRows: int) -> str:
        # Edge cases: 1 row or string length is less than numRows (no zigzag path needed)
        if numRows == 1 or numRows >= len(s):
            return s

        # Initialize array of strings for each row
        rows = [''] * numRows
        current_row = 0
        going_down = False

        # Traverse each character in the string
        for char in s:
            rows[current_row] += char
            
            # Change direction at top or bottom boundary
            if current_row == 0 or current_row == numRows - 1:
                going_down = not going_down
            
            # Move up or down based on current direction
            current_row += 1 if going_down else -1

        # Combine all rows into a single string
        return ''.join(rows)
